image: docker:stable

options:
  docker: true

pipelines:
  default:
    - step:
        script:
          - ./cidependencies.sh
          - docker-compose build
          - docker-compose up
          - docker-compose down
